const ARITMETIC_OPERATORS = ['+', '-', '*', '/'];

const LOGIGAL_OPERATORS = ['and', 'or', 'not'];

const COMPARISON_OPERATORS = ['=', '!=', '<', '>', '<=', '>='];

const BASIC_TEXT_FUNCTION = ['left', 'right', 'len', 'substring'];

const BASIC_MATH_FUNCTION = ['min', 'max', 'abs', 'sign', 'round'];

const BASIC_MISCELANEOUS = ['datediff', 'now'];

export {
  ARITMETIC_OPERATORS,
  LOGIGAL_OPERATORS,
  COMPARISON_OPERATORS,
  BASIC_TEXT_FUNCTION,
  BASIC_MATH_FUNCTION,
  BASIC_MISCELANEOUS
};
